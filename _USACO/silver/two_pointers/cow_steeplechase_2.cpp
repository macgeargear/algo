#include <iostream>
using namespace std;
using ll = long long;

struct Point {
  int x, y;
  Point(int xst, int yst) : x(xst), y(yst) {}
}

int sign(Point A, Point B, Point C) {
}

int main() {
  int n;
  cin >> n;
  for (int i = 0; i < n; i++) {
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;
  }
}
