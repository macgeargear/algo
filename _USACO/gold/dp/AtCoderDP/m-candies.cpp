#include <iostream>
#include <vector>

#define vec vector

using namespace std;

const int MOD = 1e9 + 7;

int n, k;

int solve(vec<int> &nums) {
    return 0;
}

int main() {
    cin >> n >> k;
    vec<int> nums(n);
    for (int &x : nums)
        cin >> x;
}
